class Solution:
    def findDuplicates(self, nums: List[int]) -> List[int]:
        num_counter=Counter(nums)
        r=[]
        for i in num_counter:
            if num_counter[i]==2:
                r.append(i)
        return r

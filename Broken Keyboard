t = int(input())

for _ in range(t):
    s=input().strip()
    nm=set()
    i=0
    while i<len(s):
        if i+1<len(s) and s[i] == s[i + 1]:
            i += 2 
        else:
            nm.add(s[i]) #definitely nm
            i += 1
    
    result = ''.join(sorted(nm))
    print(result)

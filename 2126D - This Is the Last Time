t=int(input())
for _ in range(t):
    n,k=map(int,input().split())
    casino=[]
    for _ in range(n):
        l,r,real=map(int,input().split())
        casino.append((l,r,real))
    casino.sort()
    for l,r,real in casino:
        if l<=k<=r:
            k=max(k,real)
    print(k)

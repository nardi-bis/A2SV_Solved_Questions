class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        count = defaultdict(list)
        for ch in strs:
            key = "".join(sorted(ch))
            count[key].append(ch)
            
        return list(count.values())
